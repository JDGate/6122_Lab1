#pragma once
#include <SFML/Graphics.hpp>

/*
    The class is responsible for maintaining the texture, location, and displaying of the
    Buzzy player (Florida man is buzzed lol).
    The class is also responsible for detecting collisions with other objects and taking
    appropriate action.
*/
class ECE_Buzzy : public sf::Sprite
{
private:


public:
    // Constructors
    ECE_Buzzy(sf::Texture& texturePlayer);

    // Getters and Setters
	

    // Member Function List
    
};