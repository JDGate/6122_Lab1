#pragma once
#include <SFML/Graphics.hpp>

/*
    The class is responsible for maintaining the texture, location, and displaying of the
    Enemy player (poor Wendy's).
    The class is also responsible for detecting collisions with other objects and taking
    appropriate action.
*/
class ECE_Enemy : public sf::Sprite
{
private:

public:
    // Constructors
    ECE_Enemy(sf::Texture& texturePlayer);

    // Getters and Setters


    // Member Function List

};