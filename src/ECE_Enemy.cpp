#include "../include/ECE_Enemy.hpp"
#include <iostream>

ECE_Enemy::ECE_Enemy(sf::Texture& texturePlayer) : sf::Sprite(texturePlayer)
{
    //std::cout << "Wendy's scale and position set" << std::endl;
}